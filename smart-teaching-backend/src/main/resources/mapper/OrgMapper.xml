<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartteaching.mapper.OrgMapper">

    <!-- 学院查询 -->
    <select id="selectCollegeList" resultType="com.smartteaching.entity.org.College">
        SELECT id,name,code,parent_id,sort,status,create_time,update_time
        FROM org_college
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY sort ASC
    </select>

    <!-- 专业查询 -->
    <select id="selectMajorList" resultType="com.smartteaching.entity.org.Major">
        SELECT id,college_id,name,code,sort,status,create_time,update_time
        FROM org_major
        <where>
            <if test="collegeId != null">
                college_id = #{collegeId}
            </if>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY sort ASC
    </select>

    <!-- 班级查询,统计人数 -->
    <select id="selectClassList" resultType="com.smartteaching.entity.org.ClassInfo">
        SELECT
        c.id,c.major_id,c.name,c.code,c.sort,c.status,c.grade_year,c.create_time,c.update_time,
        COUNT(u.id) AS studentCount
        FROM org_class c
        LEFT JOIN sys_user u ON c.id = u.class_id AND u.role = 'student' AND u.status = 1
        <where>
            <if test="majorId != null">
                c.major_id = #{majorId}
            </if>
            <if test="status != null">
                c.status = #{status}
            </if>
        </where>
        GROUP BY c.id
        ORDER BY c.sort ASC
    </select>


    <!-- ========== UserExcelValidateUtil ========== -->

    <select id="getCollegeIdByName" resultType="java.lang.Long">
        SELECT id FROM org_college WHERE name = #{name} AND status = 1
    </select>

    <select id="getMajorIdByName" resultType="java.lang.Long">
        SELECT id FROM org_major WHERE name = #{name} AND status = 1
    </select>

    <select id="getClassIdByName" resultType="java.lang.Long">
        SELECT id FROM org_class WHERE name = #{name} AND status = 1
    </select>
    <!-- 批量查询 -->
    <select id="getCollegesByNames" resultType="com.smartteaching.entity.org.College">
        SELECT id, name FROM org_college
        WHERE status = 1
        AND name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

    <select id="getMajorsByNames" resultType="com.smartteaching.entity.org.Major">
        SELECT id, name FROM org_major
        WHERE status = 1
        AND name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

    <select id="getClassesByNames" resultType="com.smartteaching.entity.org.ClassInfo">
        SELECT id, name FROM org_class
        WHERE status = 1
        AND name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>


    <!-- 批量插入 -->
    <insert id="batchInsertColleges" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO org_college (name, code, sort, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.code}, #{item.sort}, 1, NOW(), NOW())
        </foreach>
    </insert>

    <insert id="batchInsertMajors" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO org_major (college_id, name, code, sort, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.collegeId}, #{item.name}, #{item.code}, #{item.sort}, 1, NOW(), NOW())
        </foreach>
    </insert>

    <insert id="batchInsertClasses" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO org_class (major_id, name, code, grade_year, sort, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.majorId}, #{item.name}, #{item.code}, #{item.gradeYear}, #{item.sort}, 1, NOW(), NOW())
        </foreach>
    </insert>

</mapper>
